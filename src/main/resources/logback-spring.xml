<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ========================================= -->
    <!-- 1) 콘솔 로그 (컬러 적용)                    -->
    <!-- ========================================= -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Seoul} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>


    <!-- ========================================= -->
    <!-- 2) 전체 로그 파일 (WARN 이상)              -->
    <!-- ========================================= -->
    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Seoul} %-5level [%thread] %logger - %msg%n</pattern>
        </encoder>
    </appender>


    <!-- ========================================= -->
    <!-- 3) 비즈니스 로그 파일 (너의 코드만 따로)   -->
    <!-- ========================================= -->
    <appender name="BUSINESS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/business.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/business.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Seoul} %-5level [%thread] %logger - %msg%n</pattern>
        </encoder>
    </appender>


    <!-- ========================================= -->
    <!-- 4) root 로그 → WARN 이상만 기록            -->
    <!-- ========================================= -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APP_FILE"/>
    </root>


    <!-- ========================================= -->
    <!-- 5) 네 애플리케이션 로그만 INFO로 기록      -->
    <!-- ========================================= -->
    <logger name="com.lois" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="BUSINESS_FILE"/>
    </logger>

</configuration>