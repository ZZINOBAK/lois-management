<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lois.management.mapper.ReservationMapper">

    <select id="findAll" resultType="Reservation">
        SELECT r.id,
        r.res_date,
        r.res_time,
        r.cake_id,
        c.flavor AS cake_flavor,
        r.cake_size,
        r.contact
        FROM reservations r
        JOIN cakes c ON r.cake_id = c.id
        ORDER BY r.id DESC
    </select>

    <select id="findAllCakeFlavor" resultType="Cake">
        SELECT id, flavor
        FROM cakes
    </select>

    <select id="findById" parameterType="long" resultType="Reservation">
        SELECT
        id,
        res_date  AS resDate,
        res_time  AS resTime,
        cake_id   AS cakeId,
        cake_size AS cakeSize,
        contact,
        note,
        is_paid   AS paid
        FROM reservations
        WHERE id = #{id}
    </select>

    <select id="sortByPickUpTime" resultType="Reservation">
        SELECT id, res_date, res_time, cake_id, cake_size, contact
        FROM reservations
        WHERE id = #{id}
    </select>

    <select id="findTodayOrderByPickUpTime" resultType="Reservation">
        SELECT
        r.id,
        r.res_date  AS resDate,
        r.res_time  AS resTime,
        r.cake_id   AS cakeId,
        c.flavor    AS cakeFlavor,
        r.cake_size AS cakeSize,
        r.contact   AS contact
        FROM reservations r
        JOIN cakes c ON r.cake_id = c.id
        WHERE r.res_date = CURRENT_DATE()
        ORDER BY r.res_time ASC
    </select>

    <select id="findAllOrderByPickUpTime" resultType="Reservation">
        SELECT
        r.id,
        r.res_date  AS resDate,
        r.res_time  AS resTime,
        r.cake_id   AS cakeId,
        c.flavor    AS cakeFlavor,
        r.cake_size AS cakeSize,
        r.contact   AS contact
        FROM reservations r
        JOIN cakes c ON r.cake_id = c.id
        ORDER BY r.res_date ASC, r.res_time ASC
    </select>

    <insert id="insert" parameterType="Reservation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO reservations (res_date, res_time, cake_id, contact, is_paid)
        VALUES (#{resDate}, #{resTime}, #{cakeId}, #{contact}, #{paid})
    </insert>

    <update id="update" parameterType="com.lois.management.domain.Reservation"> <!-- ✅ id=update -->
        UPDATE reservations
        <set>
            <if test="resDate != null">res_date = #{resDate},</if>
            <if test="resTime != null">res_time = #{resTime},</if>
            <if test="contact != null">contact = #{contact},</if>
            <if test="paid != null">is_paid = #{paid},</if>
            <if test="status != null">status = #{status},</if>
            <if test="note != null">note = #{note},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="update1" parameterType="long">
        UPDATE reservations
        <set>
            <if test="resDate != null">res_date = #{resDate},</if>
            <if test="resTime != null">res_time = #{resTime},</if>
            <if test="contact != null">contact = #{contact},</if>
            <if test="paid != null">is_paid = #{paid},</if>
            <if test="status != null">status = #{status},</if>
            <if test="note != null">note = #{note},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="pickedUp" parameterType="long">
        UPDATE reservations
        pickuptime = #{time}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM reservations
        WHERE id = #{id}
    </delete>

</mapper>